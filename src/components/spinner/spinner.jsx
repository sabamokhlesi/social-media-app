import React from'react'
import './spinner.scss'

const spinner = (props) => {
    return (
        <div className='loader-spinner'>
            Loading...
        </div>
    )
}

export default spinner